import 'package:flutter/material.dart';

class CustomSpaceBar extends StatelessWidget {
  const CustomSpaceBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
